spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:5432/sep_order_db
    username: postgres
    password: YOUR_PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083
